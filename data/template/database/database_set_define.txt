void celeritas::${class_name}::set_${entity}(traits::param_type::${entity_type} ${entity})
{
    if (${entity} != get_${entity}())
    {
        ${entity}_.set_value(${entity});

        add_modify(${entity}_describe, get_${entity}());
    }
}