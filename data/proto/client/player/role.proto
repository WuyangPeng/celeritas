syntax = "proto3";

package celeritas.proto.client;

message role_response {
    string surname = 1;
    string name = 2;
}

message client_role_response {
  oneof payload {
    role_response role = 1;
  }
}
